let width = prompt("Volume of the box, enter width", 20);
let height = prompt("Volume of the box, enter height", 10);
let length = prompt("Volume of the box, enter length", 50);
let volume = width * height * length;
alert(`Calculated box volume is ${volume}`);
// let decision = window.confirm("Is it OK?");
// console.log(decision);
